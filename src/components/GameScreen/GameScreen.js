import React from 'react';
// import PropTypes from 'prop-types';

const GameScreen = ({ children }) => <div className="game-screen">{children}</div>;

// GameScreen.propTypes = {};

export default GameScreen;
